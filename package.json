{
  "name": "Attempter",
  "version": "0.0.1",
  "description": "A module that attempts to perform actions of the same type, and retries them on failure",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Cedotal <andrew.cedotal@gmail.com>",
  "dependencies": {
    "bluebird": "^2.9.34",
    "ioredis": "^1.7.1",
    "js-priority-queue": "^0.1.2"
  },
  "devDependencies": {
    "request-promise": "^0.4.3"
  }
}
